package cracking_coding_interview;

public class IntervewQuestions {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
	}

	public static String splitString(String str, String delimiter, int interval){
		String result = "";
		// should return the string provided, split with the given delimiter at given interval.
		// There should be no trailing delimiters at the end of the word.
		// e.g. splitString("banana", "-", 2) should return "ba-na-na"
		return result;
	}

	public static boolean fuzzyMatch(String string1, String string2){
		boolean result = false;
		// Tests if two string are mostly identical with one "bad" character allowance.
		// e.g.   fuzzyMatch("banana", "banona")  should return true.

		return result;
	}

	public static String reverseMe(String str){
		String result = "";
		// should return a reversed string.
		return result;
	}

	public boolean isPrime(int incoming){
		return false;
	}

  // returns the last word in an string of words.
	// lastWord("the blue sky plane") should return 'plane'
	public String lastWord(String incoming){
		return "";
	}

  // return the tip amount only.  tipCalc(10.00) should return 2.0
	// don't worry about currency format ... we'll cover that after
	// we talk about using packages  
	public double tipCalc(double incoming, double tip_percentage){
		return 0.0;
	}

	public String getPenultimateNode(Node node_in){
		// accepts the FIRST NODE OF A SINGLY LINKED LIST, and then returns the
		// value of the penultimate Node in that list.
		// PROVIDE STUDENTS WITH NODE CLASS on whiteboard before beginning. (Node.java)

		//return node_in.data;
		return "";
	}

}
